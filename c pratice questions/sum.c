#include <stdio.h> 


int main()
{
    int a,b;
    printf("enter the tree numbers");
    scanf("%d %d",&a,&b);
    printf("the sum of two numbers is%d",a+b);
    printf("the average of two number is%d",(a+b)/2);
    return 0;
}